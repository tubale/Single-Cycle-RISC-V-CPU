TOP = cpu


# ==========================================================
# Default / Regression Build
# ==========================================================

all:
	iverilog -o sim.out rtl/*.v sim/$(TOP)_tb.v


# ==========================================================
# Run Regression Simulation
# ==========================================================

run:
	vvp sim.out


# ==========================================================
# GTKWave Demo
# ==========================================================

waves:
	rm -f wave.out wave.vcd
	iverilog -o wave.out rtl/*.v sim/cpu_wave_tb.v
	vvp wave.out
	gtkwave wave.vcd


# ==========================================================
# Clean
# ==========================================================

clean:
	rm -f sim.out wave.out wave.vcd